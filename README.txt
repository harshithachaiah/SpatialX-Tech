# To run the file python app.py
# Default server http://127.0.0.1:5000/v1/event
# Using postman POST the object given in the SpatialX.postman_collection 
# Different test has been created in test_app.py 
# views has the routes
# utils has the generic functions such as notify user etc
# SpatialX.postman_collection is a json which was used to test all the senarios change the timestamp
# models has the temparory data structure 